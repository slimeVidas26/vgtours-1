import React, { Fragment } from 'react'

function SignUpWith({children}) {
    return (
        <Fragment>
        <i className="icon icon-logo-facebook2"></i>
            <span>{children}</span> 
        </Fragment>
    )
}

export default SignUpWith
